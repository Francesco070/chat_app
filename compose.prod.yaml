version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - MIX_ENV=prod
      - PORT=4040
    ports:
      - "4040:4040"
    restart: unless-stopped
    networks:
      - chat_network

networks:
  chat_network:
    driver: bridge